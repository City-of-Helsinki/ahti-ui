extend type FeatureProperties {
  type: String! @examples(values: ["myhelsinki"])
  header: String! @fake(type: lorem, options: {loremSize: words})
  website: String! @fake(type: url)
  imageId: Int! @fake(type: number, options: {minNumber: 7, maxNumber: 22})
}
